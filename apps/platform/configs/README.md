# 配置目录已迁移

运行配置与数据集契约统一放在**仓库根** `configs/`：

- 运行配置：`../../configs/runtime/`（`odp-gen-config train`）
- 数据集 yaml：优先 `../../data/processed/`，可选 `../../configs/datasets/`

详见 [docs/ARCHITECTURE.md](../../docs/ARCHITECTURE.md)。
